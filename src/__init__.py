# Python package root for this repo (run modules as python -m src.runInference, streamlit run src/dashboard.py).
# Submodules: paths, llmClient, runInference, dashboard, rule_display, buildYagoSubsetFromHuggingFace, cleanOutputs.
